name: Deploy DifferentEnough to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "DifferentEnough/**"
      - ".github/workflows/deploy-different-enough-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: DifferentEnough/package-lock.json

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        working-directory: DifferentEnough
        run: npm ci

      - name: Run tests
        working-directory: DifferentEnough
        run: npm run test

      - name: Build
        working-directory: DifferentEnough
        run: npm run build

      - name: Assemble site artifact
        run: |
          rm -rf _site
          mkdir -p _site
          rsync -av \
            --exclude ".git" \
            --exclude ".github" \
            --exclude "_site" \
            --exclude "DifferentEnough/node_modules" \
            ./ _site/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
